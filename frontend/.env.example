# Address of the deployed AcademicCredential contract (output of `npx hardhat run scripts/deploy-sepolia.ts --network sepolia`)
VITE_CONTRACT_ADDRESS=0xYourDeployedContractAddress

# Pinata JWT used by src/lib/ipfs.ts to pin certificate PDFs to IPFS
VITE_PINATA_JWT=your_pinata_jwt_here
